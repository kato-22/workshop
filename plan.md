# ポモドーロタイマーアプリ 段階的実装計画

このドキュメントは、添付モック、アーキテクチャ案、実装機能一覧を踏まえて、ポモドーロタイマーアプリを無理なく段階的に実装するための計画を整理したものです。

## 1. 実装方針

機能は、次の粒度で区切って実装するのが適切です。

- 1 回の実装で責務を増やしすぎない
- 各段階の終わりで画面または API として動作確認できる
- UI、状態管理、API、永続化を一度に作り込まず、依存の薄い順に積み上げる
- テストしやすい構造を早い段階で確定させる

特にこのアプリでは、次の順番を守ると手戻りが少なくなります。

1. まず画面の骨格を作る
2. 次にブラウザ内だけで完結するタイマーを作る
3. その後で API と永続化を接続する
4. 最後にテストと拡張性を強化する

## 2. 推奨する実装粒度

実装粒度は、以下の 1 単位を目安にします。

- 1 フェーズ = ユーザーから見て意味のあるまとまり
- 1 ステップ = 1 日または 1 セッションで進めやすい小さな作業単位

この計画では、全体を 7 フェーズに分けます。

## 3. フェーズ別実装計画

## フェーズ 1. プロジェクト土台の整備

目的は、以降の実装を載せるための最小構成を揃えることです。

### 実装する内容

1. Flask アプリの起動確認
2. app.py でトップ画面ルートを定義
3. templates と static の基本配置を作成
4. index.html、style.css、JavaScript ファイルの空枠を作成
5. 推奨ディレクトリ構成の雛形を作成

### この粒度にする理由

この段階では業務ロジックを入れず、画面配信とファイル構成だけを確定させます。最初に構成を整えることで、その後の変更を小さく保てます。

### 完了条件

1. Flask が起動する
2. GET / で HTML が表示される
3. CSS と JavaScript の静的ファイルが読み込まれる

## フェーズ 2. UI モックの再現

目的は、添付画像に近い単一画面 UI を静的に再現することです。

### 実装する内容

1. タイトル領域の実装
2. モード表示の実装
3. タイマー表示の実装
4. 円形プログレスの見た目の実装
5. 開始ボタンとリセットボタンの配置
6. 今日の進捗カードの配置
7. レスポンシブに大きく崩れないレイアウト調整

### この粒度にする理由

先に見た目を固めることで、後続の JavaScript 実装時に DOM 構造が安定します。UI と状態管理を同時に実装しないことで、責務の混在を防げます。

### 完了条件

1. 添付モックに近いレイアウトが表示される
2. ボタン、タイマー、進捗カードの DOM 要素が揃う
3. JavaScript 未実装でも画面として成立する

## フェーズ 3. フロントエンド状態管理の実装

目的は、タイマーの状態遷移を DOM から分離して実装することです。

### 実装する内容

1. state.js に初期状態を定義
2. START、PAUSE、RESET、TICK、COMPLETE のイベント処理を実装
3. mode、isRunning、durationSec、remainingSec、startedAt、cycleCount を管理
4. 次モード判定の基本ロジックを実装
5. 長休憩切り替えの基礎ロジックを実装

### この粒度にする理由

この段階で reducer 的な状態遷移を先に固めると、タイマー制御と DOM 更新の実装が単純になります。フロントエンドの中心ロジックを純粋関数として切り出す重要な段階です。

### 完了条件

1. 状態遷移関数だけで開始、停止、リセット、完了を表現できる
2. DOM を触らずに状態変更を確認できる
3. 作業終了後に短休憩または長休憩へ遷移できる

## フェーズ 4. タイマー制御と UI 連携

目的は、ブラウザ上で実際に動くポモドーロタイマーを完成させることです。

### 実装する内容

1. timer.js で開始、一時停止、再開、リセットを実装
2. 開始時刻ベースで残り時間を計算
3. 完了時に COMPLETE イベントを発火
4. ui.js でモード表示、残り時間、ボタン状態を更新
5. 円形プログレス表示を状態に応じて更新
6. 初期表示時の UI 描画を実装

### この粒度にする理由

ここで初めて、状態管理と UI を接続します。フェーズ 3 までにロジックが独立していれば、表示部分は状態を描画する責務に集中できます。

### 完了条件

1. 開始ボタンでタイマーが動く
2. 一時停止と再開が正しく動く
3. リセットで初期状態に戻る
4. 経過に応じてプログレス表示が更新される
5. 作業完了時に次モードへ切り替わる

## フェーズ 5. バックエンド API の最小実装

目的は、フロントエンドが必要とする API を薄い Flask ルートで提供することです。

### 実装する内容

1. 設定取得 API の実装
2. 当日統計取得 API の仮実装
3. セッション完了記録 API の仮実装
4. リクエスト JSON の簡易バリデーション
5. エラーレスポンスの基本整備
6. settings_service、stats_service、session_service の骨格作成

### この粒度にする理由

最初から SQLite までつなぐのではなく、まず API 契約を確定させます。これによりフロントエンド接続を先に進められ、バックエンド内部実装を後で差し替えやすくなります。

### 完了条件

1. GET /api/settings が固定値を返す
2. GET /api/stats/today が仮の統計値を返す
3. POST /api/sessions が正常系で受け付ける
4. 不正入力時に 400 系レスポンスを返す

## フェーズ 6. 永続化と統計集計の実装

目的は、完了セッションを SQLite に保存し、当日進捗を実データから返せるようにすることです。

### 実装する内容

1. SessionRepository 抽象の定義
2. SQLite 実装の作成
3. sessions テーブル初期化処理
4. セッション保存処理
5. 今日の完了件数集計
6. 今日の集中時間集計
7. stats_service と session_service を Repository 経由に変更
8. フロントエンドから API 経由で進捗取得と完了記録送信を接続

### この粒度にする理由

この段階でようやくデータの永続化を導入します。UI と API の契約が先に固まっていれば、SQLite の導入は内部差し替えとして進められます。

### 完了条件

1. 作業セッション完了時に記録が保存される
2. 画面再読込後も当日の進捗が取得できる
3. 今日の完了件数と集中時間が実データから計算される

## フェーズ 7. テストと保守性の強化

目的は、設計方針どおりにテストしやすい構成へ仕上げることです。

### 実装する内容

1. timer_calculator.py のユニットテスト
2. timer_rules.py のユニットテスト
3. stats_calculator.py のユニットテスト
4. Service 層のユニットテスト
5. Flask API の統合テスト
6. 不正入力ケースのテスト
7. Clock 抽象、SystemClock、FakeClock の導入
8. InMemory Repository の導入
9. フロントエンド state.js の状態遷移テスト

### この粒度にする理由

テストは最後にまとめて追加するのではなく、ドメインとサービスの責務が見えた段階で整理して入れるべきです。このフェーズで設計の妥当性も検証できます。

### 完了条件

1. 主要なドメインロジックに単体テストがある
2. API の正常系と異常系が統合テストで確認できる
3. 時刻依存と永続化依存を差し替えてテストできる

## 4. フェーズごとの実施順序

最初の実装順序は次の通りです。

1. フェーズ 1: プロジェクト土台の整備
2. フェーズ 2: UI モックの再現
3. フェーズ 3: フロントエンド状態管理の実装
4. フェーズ 4: タイマー制御と UI 連携
5. フェーズ 5: バックエンド API の最小実装
6. フェーズ 6: 永続化と統計集計の実装
7. フェーズ 7: テストと保守性の強化

## 5. 実装の進め方に関する補足

### 小さく確認しながら進める対象

1. HTML/CSS の見た目
2. JavaScript の状態遷移
3. タイマーの進行と完了
4. Flask API のレスポンス
5. SQLite 保存と集計結果

### 各フェーズで避けるべき進め方

1. UI と API と DB を同時に作り込むこと
2. app.py に業務ロジックや SQL を直接書くこと
3. state 管理と DOM 操作を同じ関数に詰め込むこと
4. 永続化前に集計値を別管理で持ってしまうこと

## 6. 最初の着手単位として特におすすめの順序

最初の数回の作業は、さらに次の小さな単位に分けると進めやすくなります。

1. Flask で index.html を返す
2. モックに近い HTML/CSS を作る
3. state.js に初期状態と reducer を作る
4. timer.js で開始と tick を動かす
5. ui.js で時間表示とボタン状態を更新する
6. settings API を返す
7. sessions API を受ける
8. SQLite 保存をつなぐ

この順序で進めると、各ステップが短く、問題が起きたときの切り分けもしやすくなります。